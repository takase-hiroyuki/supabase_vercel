// 要件、RPC関数一覧.txt

以下の SQL 文を実行する

SELECT 
    p.proname AS function_name,
    pg_get_functiondef(p.oid) AS function_definition
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname = 'public'
ORDER BY p.proname;

結果は、以下の JSON となった

[
  {
    "function_name": "borrow_bank_loan",
    "function_definition": "CREATE OR REPLACE FUNCTION public.borrow_bank_loan(p_room_id text, p_user_id text, p_borrow_amount integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_current_turn_user_id TEXT;\n    \n    v_financials JSONB;\n    v_current_cash INT;\n    v_current_loan INT;\n    v_current_interest INT;\n    \n    v_new_cash INT;\n    v_new_loan INT;\n    v_added_interest INT;\n    v_new_interest INT;\n    \n    v_total_income INT;\n    v_total_expenses INT;\n    v_net_cash_flow INT;\nBEGIN\n    IF p_borrow_amount <= 0 OR (p_borrow_amount % 1000) != 0 THEN\n        RETURN jsonb_build_object('success', false, 'error', '銀行ローンは1,000ドル単位でのみ借り入れ可能です。');\n    END IF;\n\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    SELECT id, state INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    v_financials := v_state->'financials';\n    v_current_cash := (v_financials->>'cash')::INT;\n    \n    IF v_financials->'liabilities' IS NULL THEN\n        v_financials := jsonb_set(v_financials, '{liabilities}', '{}'::jsonb);\n    END IF;\n    v_current_loan := COALESCE((v_financials->'liabilities'->>'bank_loan')::INT, 0);\n\n    IF v_financials->'expenses' IS NULL THEN\n        v_financials := jsonb_set(v_financials, '{expenses}', '{}'::jsonb);\n    END IF;\n    v_current_interest := COALESCE((v_financials->'expenses'->>'loan_interest')::INT, 0);\n\n    v_new_cash := v_current_cash + p_borrow_amount;\n    v_new_loan := v_current_loan + p_borrow_amount;\n    v_added_interest := (p_borrow_amount / 1000) * 100;\n    v_new_interest := v_current_interest + v_added_interest;\n\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_new_cash));\n    v_financials := jsonb_set(v_financials, '{liabilities, bank_loan}', to_jsonb(v_new_loan));\n    v_financials := jsonb_set(v_financials, '{expenses, loan_interest}', to_jsonb(v_new_interest));\n\n    -- 正しいネスト構造へのアクセスと更新\n    v_total_income := (v_financials->'income'->>'total')::INT;\n    v_total_expenses := (v_financials->'expenses'->>'total')::INT + v_added_interest;\n    v_net_cash_flow := v_total_income - v_total_expenses;\n\n    v_financials := jsonb_set(v_financials, '{expenses, total}', to_jsonb(v_total_expenses));\n    v_financials := jsonb_set(v_financials, '{cashflow}', to_jsonb(v_net_cash_flow));\n\n    v_state := jsonb_set(v_state, '{financials}', v_financials);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE id = v_participant_id;\n\n    RETURN jsonb_build_object(\n        'success', true,\n        'borrow_amount', p_borrow_amount,\n        'added_interest', v_added_interest,\n        'new_cash', v_new_cash,\n        'new_bank_loan', v_new_loan,\n        'new_loan_interest', v_new_interest,\n        'new_net_cash_flow', v_net_cash_flow,\n        'message', '銀行からの借り入れが正常に完了し、利息支出とキャッシュフローが再計算されました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "buy_real_estate_asset",
    "function_definition": "CREATE OR REPLACE FUNCTION public.buy_real_estate_asset(p_room_id text, p_user_id text, p_asset_name text, p_cost integer, p_down_payment integer, p_mortgage integer, p_passive_income integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_room_record RECORD;\n    v_game_state JSONB;\n    v_current_card JSONB;\n    v_card_id INT;\n    v_deck_type TEXT;\n    v_graveyard JSONB;\n    v_deck_graveyard JSONB;\n    v_financials JSONB;\n    v_current_cash INT;\n    v_new_cash INT;\n    v_passive_total INT;\n    v_income_total INT;\n    v_expenses_total INT;\n    v_new_cashflow INT;\n    v_new_property JSONB;\n    v_real_estate_list JSONB;\nBEGIN\n    -- 1. 手番チェックと部屋データの取得（行ロック）\n    SELECT * INTO v_room_record FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room_record.current_turn_user_id IS NULL OR v_room_record.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    v_game_state := v_room_record.game_state;\n\n    -- 2. プレイヤーデータ取得\n    SELECT id, state INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    -- 3. フェーズ確認\n    IF v_state->>'calculation_phase' NOT IN ('small_deal', 'big_deal') THEN\n        RETURN jsonb_build_object('success', false, 'error', '現在は資産を購入できるフェーズではありません。');\n    END IF;\n\n    -- 4. 財務データの抽出とキャッシュ確認\n    v_financials := v_state->'financials';\n    v_current_cash := COALESCE((v_financials->>'cash')::INT, 0);\n\n    IF v_current_cash < p_down_payment THEN\n        RETURN jsonb_build_object('success', false, 'error', '手元資金（キャッシュ）が足りないため、この物件は購入できません。');\n    END IF;\n\n    -- 5. 購入処理（キャッシュの減算）\n    v_new_cash := v_current_cash - p_down_payment;\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_new_cash));\n\n    -- 6. 不動産リストへの追加\n    v_real_estate_list := v_financials->'assets'->'real_estate';\n    IF v_real_estate_list IS NULL THEN\n        v_real_estate_list := '[]'::jsonb;\n    END IF;\n\n    v_new_property := jsonb_build_object(\n        'name', p_asset_name,\n        'cost', p_cost,\n        'down_payment', p_down_payment,\n        'mortgage', p_mortgage,\n        'passive_income', p_passive_income\n    );\n    v_real_estate_list := v_real_estate_list || v_new_property;\n    v_financials := jsonb_set(v_financials, '{assets, real_estate}', v_real_estate_list);\n\n    -- 7. 財務諸表の自動再計算\n    v_passive_total := COALESCE((v_financials->'income'->>'passive')::INT, 0) + p_passive_income;\n    v_financials := jsonb_set(v_financials, '{income, passive}', to_jsonb(v_passive_total));\n\n    v_income_total := COALESCE((v_financials->'income'->>'salary')::INT, 0) + v_passive_total;\n    v_financials := jsonb_set(v_financials, '{income, total}', to_jsonb(v_income_total));\n\n    v_expenses_total := COALESCE((v_financials->'expenses'->>'total')::INT, 0);\n    v_new_cashflow := v_income_total - v_expenses_total;\n    v_financials := jsonb_set(v_financials, '{cashflow}', to_jsonb(v_new_cashflow));\n\n    -- 8. ステータスの更新とフェーズの初期化\n    v_state := jsonb_set(v_state, '{financials}', v_financials);\n    v_state := jsonb_set(v_state, '{calculation_phase}', '\"none\"'::jsonb);\n    v_state := jsonb_set(v_state, '{is_calculating}', 'false'::jsonb);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE id = v_participant_id;\n\n    -- 9. カードのGraveyard退避処理\n    v_current_card := v_game_state->'current_card';\n    IF v_current_card IS NOT NULL AND v_current_card->>'status' = 'active' THEN\n        v_deck_type := v_current_card->>'deck_type';\n        v_card_id := (v_current_card->>'card_id')::INT;\n        \n        v_graveyard := COALESCE(v_game_state->'graveyard', '{\"small_deal\":[], \"big_deal\":[], \"market\":[], \"doodad\":[]}'::JSONB);\n        v_deck_graveyard := COALESCE(v_graveyard->v_deck_type, '[]'::JSONB);\n        \n        v_deck_graveyard := v_deck_graveyard || to_jsonb(v_card_id);\n        v_graveyard := jsonb_set(v_graveyard, ARRAY[v_deck_type], v_deck_graveyard);\n        \n        v_game_state := jsonb_set(v_game_state, '{graveyard}', v_graveyard);\n        v_game_state := jsonb_set(v_game_state, '{current_card, status}', '\"completed\"'::JSONB);\n\n        UPDATE rooms\n        SET game_state = v_game_state\n        WHERE id = p_room_id;\n    END IF;\n\n    RETURN jsonb_build_object(\n        'success', true,\n        'purchased_asset', p_asset_name,\n        'remaining_cash', v_new_cash,\n        'new_passive_income', v_passive_total,\n        'new_cashflow', v_new_cashflow,\n        'message', '物件の購入が正常に完了し、カードを退避させました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "buy_stock_asset",
    "function_definition": "CREATE OR REPLACE FUNCTION public.buy_stock_asset(p_room_id text, p_user_id text, p_stock_symbol text, p_buy_price integer, p_quantity integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_room_record RECORD;\n    v_game_state JSONB;\n    v_current_card JSONB;\n    v_card_id INT;\n    v_deck_type TEXT;\n    v_graveyard JSONB;\n    v_deck_graveyard JSONB;\n    \n    v_financials JSONB;\n    v_current_cash INT;\n    v_total_cost INT;\n    v_new_cash INT;\n    \n    v_stocks_list JSONB;\n    v_existing_stock JSONB;\n    v_current_quantity INT := 0;\n    \n    v_current_avg_price NUMERIC := 0; \n    v_new_quantity INT;\n    v_new_avg_price NUMERIC; \nBEGIN\n    IF p_quantity <= 0 OR p_buy_price <= 0 THEN\n        RETURN jsonb_build_object('success', false, 'error', '株数および購入価格は1以上である必要があります。');\n    END IF;\n\n    SELECT * INTO v_room_record FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room_record.current_turn_user_id IS NULL OR v_room_record.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    v_game_state := v_room_record.game_state;\n\n    SELECT id, state INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF NOT (v_state->>'is_calculating')::BOOLEAN OR v_state->>'calculation_phase' != 'small_deal' THEN\n        RETURN jsonb_build_object('success', false, 'error', '現在は株式を購入できるフェーズではありません。');\n    END IF;\n\n    v_financials := v_state->'financials';\n    v_current_cash := (v_financials->>'cash')::INT;\n    v_total_cost := p_buy_price * p_quantity;\n\n    IF v_current_cash < v_total_cost THEN\n        RETURN jsonb_build_object('success', false, 'error', '手元資金（キャッシュ）が足りないため、この株は購入できません。');\n    END IF;\n\n    v_new_cash := v_current_cash - v_total_cost;\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_new_cash));\n\n    v_stocks_list := v_financials->'assets'->'stocks';\n    IF v_stocks_list IS NULL THEN\n        v_stocks_list := '{}'::jsonb;\n    END IF;\n\n    v_existing_stock := v_stocks_list->p_stock_symbol;\n\n    IF v_existing_stock IS NOT NULL THEN\n        v_current_quantity := (v_existing_stock->>'quantity')::INT;\n        v_current_avg_price := (v_existing_stock->>'average_price')::NUMERIC;\n        \n        v_new_quantity := v_current_quantity + p_quantity;\n        v_new_avg_price := ((v_current_avg_price * v_current_quantity) + v_total_cost) / v_new_quantity;\n    ELSE\n        v_new_quantity := p_quantity;\n        v_new_avg_price := p_buy_price::NUMERIC;\n    END IF;\n\n    v_stocks_list := jsonb_set(v_stocks_list, ARRAY[p_stock_symbol], jsonb_build_object(\n        'quantity', v_new_quantity,\n        'average_price', v_new_avg_price\n    ));\n\n    v_financials := jsonb_set(v_financials, '{assets, stocks}', v_stocks_list);\n\n    v_state := jsonb_set(v_state, '{financials}', v_financials);\n    v_state := jsonb_set(v_state, '{is_calculating}', 'false'::jsonb);\n    v_state := jsonb_set(v_state, '{calculation_phase}', '\"none\"'::jsonb);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE id = v_participant_id;\n\n    -- カードのGraveyard退避処理\n    v_current_card := v_game_state->'current_card';\n    IF v_current_card IS NOT NULL AND v_current_card->>'status' = 'active' THEN\n        v_deck_type := v_current_card->>'deck_type';\n        v_card_id := (v_current_card->>'card_id')::INT;\n        \n        v_graveyard := COALESCE(v_game_state->'graveyard', '{\"small_deal\":[], \"big_deal\":[], \"market\":[], \"doodad\":[]}'::JSONB);\n        v_deck_graveyard := COALESCE(v_graveyard->v_deck_type, '[]'::JSONB);\n        \n        v_deck_graveyard := v_deck_graveyard || to_jsonb(v_card_id);\n        v_graveyard := jsonb_set(v_graveyard, ARRAY[v_deck_type], v_deck_graveyard);\n        \n        v_game_state := jsonb_set(v_game_state, '{graveyard}', v_graveyard);\n        v_game_state := jsonb_set(v_game_state, '{current_card, status}', '\"completed\"'::JSONB);\n\n        UPDATE rooms\n        SET game_state = v_game_state\n        WHERE id = p_room_id;\n    END IF;\n\n    RETURN jsonb_build_object(\n        'success', true,\n        'stock_symbol', p_stock_symbol,\n        'quantity_bought', p_quantity,\n        'total_cost', v_total_cost,\n        'remaining_cash', v_new_cash,\n        'new_total_quantity', v_new_quantity,\n        'average_price', v_new_avg_price,\n        'message', '株式の購入が正常に完了し、カードを退避させました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "check_and_apply_financials",
    "function_definition": "CREATE OR REPLACE FUNCTION public.check_and_apply_financials(p_room_id text, p_user_id text, p_input_total_income integer, p_input_net_cashflow integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_financials JSONB;\n    \n    -- データベース内の真実の数値\n    v_salary INT;\n    v_passive_income INT;\n    v_total_expenses INT;\n    v_current_cash INT;\n    \n    -- サーバー側での厳格な計算結果\n    v_correct_total_income INT;\n    v_correct_net_cashflow INT;\n    v_new_cash INT;\nBEGIN\n    -- 1. 行ロック（FOR UPDATE）をかけ、他関数の同時割り込みを完全に遮断\n    SELECT id, state \n    INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    -- 参加者の存在チェック\n    IF v_participant_id IS NULL THEN\n        RETURN jsonb_build_object('success', false, 'error', '指定されたプレイヤーが見つかりません。');\n    END IF;\n\n    -- 2. 現在ロック状態（計算中）であるか確認\n    IF NOT (v_state->>'is_calculating')::BOOLEAN THEN\n        RETURN jsonb_build_object('success', false, 'error', '現在は財務計算フェーズではありません。');\n    END IF;\n\n    -- 3. JSONB構造の深い階層から「真実の財務データ」を抽出\n    v_financials    := v_state->'financials';\n    v_salary        := (v_financials->'income'->>'salary')::INT;\n    v_passive_income:= (v_financials->'income'->>'passive')::INT;\n    v_total_expenses:= (v_financials->'expenses'->>'total')::INT;\n    v_current_cash  := (v_financials->>'cash')::INT;\n\n    -- 4. サーバー側（データベース内部）で絶対的な正解をスタンドアロン計算\n    v_correct_total_income := v_salary + v_passive_income;\n    v_correct_net_cashflow := v_correct_total_income - v_total_expenses;\n\n    -- 5. プレイヤーが電卓を叩いて入力した値と、内部の正解を厳格に突き合わせ\n    IF p_input_total_income != v_correct_total_income OR p_input_net_cashflow != v_correct_net_cashflow THEN\n        -- 1円でも違えばデータを一切変更（汚染）せず即座にエラーを返却\n        RETURN jsonb_build_object(\n            'success', false, \n            'error', '財務諸表の計算が一致しません。電卓の入力内容、またはPL/BSの計算を再確認してください。'\n        );\n    END IF;\n\n    -- 6. 検証合格時のみ、手持ちキャッシュに給料（Net Cashflow）を加算\n    v_new_cash := v_current_cash + v_correct_net_cashflow;\n\n    -- 7. JSONBデータを破壊しないよう、特定のキーのみをピンポイントでアトミック更新\n    -- is_calculating を false (ロック解除) にし、cash を最新値へ置換\n    UPDATE participants\n    SET state = jsonb_set(\n                    jsonb_set(state, '{is_calculating}', 'false'::jsonb),\n                    '{financials,cash}', to_jsonb(v_new_cash)\n                )\n    WHERE id = v_participant_id;\n\n    -- 8. 成功レスポンスとクリーンな最新データを返却\n    RETURN jsonb_build_object(\n        'success', true,\n        'new_cash', v_new_cash,\n        'message', '計算の正解を確認しました。手持ちキャッシュに給料が加算され、手番終了ロックが解除されました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "check_and_escape_rat_race",
    "function_definition": "CREATE OR REPLACE FUNCTION public.check_and_escape_rat_race(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_win_mode TEXT;\n    \n    v_financials JSONB;\n    v_passive_income INT;\n    v_total_expenses INT;\n    v_target_amount INT;\n    \n    v_is_escaped BOOLEAN := false;\nBEGIN\n    SELECT win_condition_mode INTO v_win_mode\n    FROM public.rooms WHERE id = p_room_id;\n\n    IF v_win_mode IS NULL THEN\n        RETURN jsonb_build_object('success', false, 'error', '部屋が見つかりません。');\n    END IF;\n\n    SELECT id, state INTO v_participant_id, v_state\n    FROM public.participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF v_participant_id IS NULL THEN\n        RETURN jsonb_build_object('success', false, 'error', 'プレイヤーが見つかりません。');\n    END IF;\n\n    IF COALESCE((v_state->>'game_phase'), 'rat_race') = 'fast_track' THEN\n        RETURN jsonb_build_object('success', true, 'escaped', true, 'message', '既にラットレースを脱出しています。');\n    END IF;\n\n    v_financials := v_state->'financials';\n    \n    -- スキーマのパス修正 (income->passive, expenses->total)\n    v_passive_income := COALESCE((v_financials->'income'->>'passive')::INT, 0);\n    v_total_expenses := COALESCE((v_financials->'expenses'->>'total')::INT, 0);\n\n    IF v_win_mode = 'double' THEN\n        v_target_amount := v_total_expenses * 2;\n    ELSE\n        v_target_amount := v_total_expenses;\n    END IF;\n\n    IF v_passive_income > v_target_amount THEN\n        v_is_escaped := true;\n        v_state := jsonb_set(v_state, '{game_phase}', '\"fast_track\"'::jsonb);\n        \n        UPDATE public.participants\n        SET state = v_state\n        WHERE id = v_participant_id;\n    END IF;\n\n    RETURN jsonb_build_object(\n        'success', true,\n        'escaped', v_is_escaped,\n        'win_mode', v_win_mode,\n        'passive_income', v_passive_income,\n        'target_amount_to_escape', v_target_amount,\n        'current_total_expenses', v_total_expenses\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "clean_participant_state",
    "function_definition": "CREATE OR REPLACE FUNCTION public.clean_participant_state()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    -- 許可リストに 'game_phase' を追加し、脱出フラグの消去を防止\n    allowed_keys TEXT[] := ARRAY[\n        'name', 'role', 'profession', 'track', 'position', 'last_dice', \n        'is_calculating', 'calculation_phase', 'children_count', \n        'charity_turns_left', 'downsized_turns_left', 'financials',\n        'game_phase'\n    ];\n    key_text TEXT;\n    cleaned_state JSONB := '{}'::jsonb;\nBEGIN\n    -- stateが設定されている場合のみ処理を実行\n    IF NEW.state IS NOT NULL AND NEW.state <> '{}'::jsonb THEN\n        FOREACH key_text IN ARRAY allowed_keys LOOP\n            IF NEW.state ? key_text THEN\n                cleaned_state := jsonb_set(cleaned_state, ARRAY[key_text], NEW.state -> key_text);\n            END IF;\n        END LOOP;\n        NEW.state := cleaned_state;\n    END IF;\n    RETURN NEW;\nEND;\n$function$\n"
  },
  {
    "function_name": "draw_card_from_deck",
    "function_definition": "CREATE OR REPLACE FUNCTION public.draw_card_from_deck(p_room_id text, p_user_id text, p_deck_type text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nDECLARE\r\n    v_room_record RECORD;\r\n    v_decks JSONB;\r\n    v_target_deck JSONB;\r\n    v_card_id INT;\r\n    v_new_deck JSONB;\r\n    v_game_state JSONB;\r\n    \r\n    -- マスターデータ取得用\r\n    v_card_record RECORD;\r\n    \r\n    -- プレイヤー状態検証用\r\n    v_participant_id INT;\r\n    v_state JSONB;\r\nBEGIN\r\n    -- 1. デッキタイプの不正値をあらかじめ防御\r\n    IF p_deck_type NOT IN ('small_deal', 'big_deal', 'market', 'doodad') THEN\r\n        RETURN jsonb_build_object('success', false, 'error', '無効なデッキタイプです。');\r\n    END IF;\r\n\r\n    -- 2. 部屋のデータをロックし、手番を厳格にチェック\r\n    SELECT * INTO v_room_record FROM rooms WHERE id = p_room_id FOR UPDATE;\r\n    IF v_room_record.current_turn_user_id IS NULL OR v_room_record.current_turn_user_id != p_user_id THEN\r\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\r\n    END IF;\r\n\r\n    -- 3. プレイヤーのデータをロックし、現在ロック中でないか確認\r\n    SELECT id, state INTO v_participant_id, v_state\r\n    FROM participants\r\n    WHERE room_id = p_room_id AND user_id = p_user_id\r\n    FOR UPDATE;\r\n\r\n    IF (v_state->>'is_calculating')::BOOLEAN THEN\r\n        RETURN jsonb_build_object('success', false, 'error', '現在の計算フェーズが完了するまでカードを引けません。');\r\n    END IF;\r\n\r\n    -- 4. 部屋の game_state から該当の山札を取り出す\r\n    v_game_state := v_room_record.game_state;\r\n    v_decks := v_game_state->'decks';\r\n    v_target_deck := v_decks->p_deck_type;\r\n\r\n    -- 5. 山札が空っぽでないかチェック\r\n    IF v_target_deck IS NULL OR jsonb_array_length(v_target_deck) = 0 THEN\r\n        RETURN jsonb_build_object('success', false, 'error', '山札がなくなりました。リシャッフルが必要です。');\r\n    END IF;\r\n\r\n    -- 6. 配列の先頭からカードIDを抽出\r\n    v_card_id := (v_target_deck->>0)::INT;\r\n\r\n    -- 7. 先頭要素を削除した新しい配列を生成\r\n    SELECT jsonb_agg(elem) INTO v_new_deck\r\n    FROM (\r\n        SELECT elem \r\n        FROM jsonb_array_elements(v_target_deck) WITH ORDINALITY AS x(elem, idx)\r\n        WHERE idx > 1\r\n    ) AS sub;\r\n\r\n    IF v_new_deck IS NULL THEN\r\n        v_new_deck := '[]'::jsonb;\r\n    END IF;\r\n\r\n    -- 8. 部屋の game_state を新しい山札データで更新\r\n    v_game_state := jsonb_set(v_game_state, ARRAY['decks', p_deck_type], v_new_deck);\r\n    \r\n    UPDATE rooms \r\n    SET game_state = v_game_state\r\n    WHERE id = p_room_id;\r\n\r\n    -- 9. 🌟 変更点: 引いたカードのマスター情報を `cards` テーブルからすべて取得\r\n    SELECT * INTO v_card_record\r\n    FROM cards\r\n    WHERE id = v_card_id;\r\n\r\n    -- 10. プレイヤーの状態を「カード処理中」のフェーズへロック移行\r\n    v_state := jsonb_set(v_state, '{is_calculating}', 'true'::jsonb);\r\n    v_state := jsonb_set(v_state, '{calculation_phase}', to_jsonb(p_deck_type));\r\n    \r\n    UPDATE participants\r\n    SET state = v_state\r\n    WHERE id = v_participant_id;\r\n\r\n    -- 11. 🌟 変更点: 引いたカードのデータと処理結果を返却（金額データ等もすべて含める）\r\n    RETURN jsonb_build_object(\r\n        'success', true,\r\n        'card_id', v_card_id,\r\n        'title', v_card_record.title,\r\n        'description', v_card_record.description,\r\n        'cost', v_card_record.cost,\r\n        'down_payment', v_card_record.down_payment,\r\n        'mortgage', v_card_record.mortgage,\r\n        'passive_income', v_card_record.passive_income,\r\n        'symbol', v_card_record.symbol,\r\n        'asset_type', v_card_record.asset_type,\r\n        'is_resellable', v_card_record.is_resellable,\r\n        'action_rule', v_card_record.action_rule,\r\n        'deck_type', p_deck_type,\r\n        'remaining_cards', jsonb_array_length(v_new_deck),\r\n        'message', 'カードをドローしました。内容に応じて売買または支払い処理を行ってください（行動ロック中）。'\r\n    );\r\nEND;\r\n$function$\n"
  },
  {
    "function_name": "jsonb_deep_merge",
    "function_definition": "CREATE OR REPLACE FUNCTION public.jsonb_deep_merge(a jsonb, b jsonb)\n RETURNS jsonb\n LANGUAGE sql\n IMMUTABLE\nAS $function$\n  SELECT\n    COALESCE(\n      jsonb_object_agg(\n        COALESCE(ka, kb),\n        CASE\n          WHEN va IS NULL THEN vb\n          WHEN vb IS NULL THEN va\n          WHEN jsonb_typeof(va) = 'object' AND jsonb_typeof(vb) = 'object' THEN public.jsonb_deep_merge(va, vb)\n          ELSE vb\n        END\n      ),\n      '{}'::jsonb\n    )\n  FROM jsonb_each(a) e1(ka, va)\n  FULL JOIN jsonb_each(b) e2(kb, vb) ON ka = kb;\n$function$\n"
  },
  {
    "function_name": "merge_participant_state",
    "function_definition": "CREATE OR REPLACE FUNCTION public.merge_participant_state(target_user_id text, state_patch jsonb)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nBEGIN\n    UPDATE public.participants\n    SET state = public.jsonb_deep_merge(COALESCE(state, '{}'::jsonb), state_patch)\n    WHERE user_id = target_user_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "pass_and_end_turn",
    "function_definition": "CREATE OR REPLACE FUNCTION public.pass_and_end_turn(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_room_record RECORD;\n    v_current_player RECORD;\n    v_next_player RECORD;\n    v_next_user_id TEXT;\n    v_max_position INT;\n    v_target_position INT;\n    v_loop_count INT := 0;\nBEGIN\n    -- 1. 部屋データをロックし、現在の手番プレイヤーか厳格にチェック\n    SELECT * INTO v_room_record FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room_record.current_turn_user_id IS NULL OR v_room_record.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    -- 2. 現在のプレイヤーの行動ロックを強制解除（パスまたは行動終了によるクリーンアップ）\n    UPDATE participants\n    SET state = jsonb_set(\n                    jsonb_set(state, '{is_calculating}', 'false'::jsonb),\n                    '{calculation_phase}', '\"none\"'::jsonb\n                )\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    RETURNING (state->>'position')::INT INTO v_target_position;\n\n    -- 3. ルーム内の最大ポジション（最大参加者数）を取得\n    SELECT MAX((state->>'position')::INT) INTO v_max_position\n    FROM participants\n    WHERE room_id = p_room_id;\n\n    -- 4. 次の手番プレイヤーを決定するループ（自己防衛のための上限ループを設定）\n    -- 基本は現在の position + 1 ですが、最大値を超えたら 1 に戻ります\n    -- 今後「自己破産やリタイアで不参加になったプレイヤー」をスキップする拡張性も担保しています\n    LOOP\n        v_target_position := v_target_position + 1;\n        IF v_target_position > v_max_position THEN\n            v_target_position := 1; -- 最初のポジションに戻る\n        END IF;\n\n        -- 指定したポジションのプレイヤーを検索\n        SELECT user_id INTO v_next_user_id\n        FROM participants\n        WHERE room_id = p_room_id AND (state->>'position')::INT = v_target_position;\n\n        -- プレイヤーが見つかればループを抜ける\n        IF v_next_user_id IS NOT NULL THEN\n            IF v_next_user_id = p_user_id THEN\n                -- 参加者が1人しかいない、または全員スキップされて自分に戻ってきた場合\n                v_next_user_id := p_user_id;\n            END IF;\n            EXIT;\n        END IF;\n\n        -- 無限ループ防御策（参加者数を超えてループしたら強制ブレイク）\n        v_loop_count := v_loop_count + 1;\n        IF v_loop_count > v_max_position THEN\n            v_next_user_id := p_user_id;\n            EXIT;\n        END IF;\n    END LOOP;\n\n    -- 5. 部屋の current_turn_user_id を次のプレイヤーへ更新\n    UPDATE rooms\n    SET current_turn_user_id = v_next_user_id\n    WHERE id = p_room_id;\n\n    -- 6. 結果を返却\n    RETURN jsonb_build_object(\n        'success', true,\n        'previous_user_id', p_user_id,\n        'next_turn_user_id', v_next_user_id,\n        'message', '手番が次のプレイヤーに正常に移行しました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "pay_off_bank_loan",
    "function_definition": "CREATE OR REPLACE FUNCTION public.pay_off_bank_loan(p_room_id text, p_user_id text, p_payback_amount integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_current_turn_user_id TEXT;\n    \n    v_financials JSONB;\n    v_current_cash INT;\n    v_current_loan INT;\n    v_current_interest INT;\n    \n    v_new_cash INT;\n    v_new_loan INT;\n    v_reduced_interest INT;\n    v_new_interest INT;\n    \n    v_total_income INT;\n    v_total_expenses INT;\n    v_net_cash_flow INT;\nBEGIN\n    IF p_payback_amount <= 0 OR (p_payback_amount % 1000) != 0 THEN\n        RETURN jsonb_build_object('success', false, 'error', '銀行ローンの返済は1,000ドル単位でのみ可能です。');\n    END IF;\n\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    SELECT id, state INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    v_financials := v_state->'financials';\n    v_current_cash := (v_financials->>'cash')::INT;\n    \n    v_current_loan := COALESCE((v_financials->'liabilities'->>'bank_loan')::INT, 0);\n    v_current_interest := COALESCE((v_financials->'expenses'->>'loan_interest')::INT, 0);\n\n    IF v_current_loan < p_payback_amount THEN\n        RETURN jsonb_build_object('success', false, 'error', '返済希望額が現在のローン残高（' || v_current_loan || 'ドル）を超えています。');\n    END IF;\n\n    IF v_current_cash < p_payback_amount THEN\n        RETURN jsonb_build_object('success', false, 'error', '手元資金（キャッシュ）が足りないため、ローンを返済できません。');\n    END IF;\n\n    v_new_cash := v_current_cash - p_payback_amount;\n    v_new_loan := v_current_loan - p_payback_amount;\n    \n    v_reduced_interest := (p_payback_amount / 1000) * 100;\n    v_new_interest := v_current_interest - v_reduced_interest;\n\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_new_cash));\n    v_financials := jsonb_set(v_financials, '{liabilities, bank_loan}', to_jsonb(v_new_loan));\n    v_financials := jsonb_set(v_financials, '{expenses, loan_interest}', to_jsonb(v_new_interest));\n\n    -- 正しいネスト構造へのアクセスと更新\n    v_total_income := (v_financials->'income'->>'total')::INT;\n    v_total_expenses := (v_financials->'expenses'->>'total')::INT - v_reduced_interest;\n    v_net_cash_flow := v_total_income - v_total_expenses;\n\n    v_financials := jsonb_set(v_financials, '{expenses, total}', to_jsonb(v_total_expenses));\n    v_financials := jsonb_set(v_financials, '{cashflow}', to_jsonb(v_net_cash_flow));\n\n    v_state := jsonb_set(v_state, '{financials}', v_financials);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE id = v_participant_id;\n\n    RETURN jsonb_build_object(\n        'success', true,\n        'payback_amount', p_payback_amount,\n        'reduced_interest', v_reduced_interest,\n        'new_cash', v_new_cash,\n        'new_bank_loan', v_new_loan,\n        'new_loan_interest', v_new_interest,\n        'new_net_cash_flow', v_net_cash_flow,\n        'message', '銀行ローンの返済が正常に完了し、利息支出が削減され、キャッシュフローが回復しました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "process_financial_transaction",
    "function_definition": "CREATE OR REPLACE FUNCTION public.process_financial_transaction(p_user_id text, p_room_id text, p_cash_delta integer, p_passive_income_delta integer, p_unlock_calc boolean)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_current_state JSONB;\n    v_financials JSONB;\n    v_current_cash INT;\n    v_current_passive INT;\n    v_current_total_income INT;\n    v_current_total_expenses INT;\n    v_current_cashflow INT;\n    v_room_record RECORD;\n    v_game_state JSONB;\n    v_current_card JSONB;\n    v_card_id INT;\n    v_deck_type TEXT;\n    v_graveyard JSONB;\n    v_deck_graveyard JSONB;\n    v_action_rule JSONB;\n    v_is_loan BOOLEAN;\n    v_loan_amount INT;\n    v_monthly_payment INT;\n    v_liabilities JSONB;\n    v_expenses JSONB;\n    v_retail_debt INT;\n    v_other_expense INT;\nBEGIN\n    -- 1. 行ロックで部屋データを取得\n    IF p_room_id IS NOT NULL THEN\n        SELECT * INTO v_room_record FROM rooms WHERE id = p_room_id FOR UPDATE;\n        v_game_state := v_room_record.game_state;\n        v_current_card := v_game_state->'current_card';\n    END IF;\n\n    -- 2. 行ロックでプレイヤーデータを取得\n    SELECT state INTO v_current_state\n    FROM participants\n    WHERE user_id = p_user_id\n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RAISE EXCEPTION 'Participant not found';\n    END IF;\n\n    v_financials := COALESCE(v_current_state->'financials', '{}'::JSONB);\n    v_current_cash := COALESCE((v_financials->>'cash')::INT, 0);\n    v_current_passive := COALESCE((v_financials->'income'->>'passive')::INT, 0);\n    v_current_total_income := COALESCE((v_financials->'income'->>'total')::INT, 0);\n    v_current_total_expenses := COALESCE((v_financials->'expenses'->>'total')::INT, 0);\n    v_current_cashflow := COALESCE((v_financials->>'cashflow')::INT, 0);\n    v_liabilities := COALESCE(v_financials->'liabilities', '{}'::JSONB);\n    v_expenses := COALESCE(v_financials->'expenses', '{}'::JSONB);\n\n    -- 3. カード情報の解析とDoodadローン処理の自動判別\n    IF v_current_card IS NOT NULL AND v_current_card->>'status' = 'active' THEN\n        v_deck_type := v_current_card->>'deck_type';\n        v_action_rule := v_current_card->'action_rule';\n\n        -- Doodadのローン決済判定\n        IF v_deck_type = 'doodad' AND v_action_rule IS NOT NULL THEN\n            v_is_loan := COALESCE((v_action_rule->>'is_loan')::BOOLEAN, false);\n            IF v_is_loan THEN\n                -- ローン処理：現金は減らさず、負債と支出を増加\n                v_loan_amount := COALESCE((v_action_rule->>'amount')::INT, 0);\n                v_monthly_payment := COALESCE((v_action_rule->>'monthly_payment')::INT, 0);\n                \n                v_retail_debt := COALESCE((v_liabilities->>'retail_debt')::INT, 0) + v_loan_amount;\n                v_other_expense := COALESCE((v_expenses->>'other')::INT, 0) + v_monthly_payment;\n                \n                v_liabilities := jsonb_set(v_liabilities, '{retail_debt}', to_jsonb(v_retail_debt));\n                v_expenses := jsonb_set(v_expenses, '{other}', to_jsonb(v_other_expense));\n                \n                v_current_total_expenses := v_current_total_expenses + v_monthly_payment;\n\n                -- Doodadローンの場合、引数の p_cash_delta は無視する（二重減算・不整合防止）\n                p_cash_delta := 0;\n            END IF;\n        END IF;\n    END IF;\n\n    -- 4. キャッシュ・不労所得・キャッシュフローの再計算\n    v_current_cash := v_current_cash + p_cash_delta;\n    v_current_passive := v_current_passive + p_passive_income_delta;\n    v_current_total_income := v_current_total_income + p_passive_income_delta;\n    v_current_cashflow := v_current_total_income - v_current_total_expenses;\n\n    -- 5. 財務データ更新\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_current_cash));\n    v_financials := jsonb_set(v_financials, '{income, passive}', to_jsonb(v_current_passive));\n    v_financials := jsonb_set(v_financials, '{income, total}', to_jsonb(v_current_total_income));\n    v_financials := jsonb_set(v_financials, '{expenses}', v_expenses);\n    v_financials := jsonb_set(v_financials, '{expenses, total}', to_jsonb(v_current_total_expenses));\n    v_financials := jsonb_set(v_financials, '{liabilities}', v_liabilities);\n    v_financials := jsonb_set(v_financials, '{cashflow}', to_jsonb(v_current_cashflow));\n\n    v_current_state := jsonb_set(v_current_state, '{financials}', v_financials);\n\n    -- 6. 計算ロック解除\n    IF p_unlock_calc THEN\n        v_current_state := jsonb_set(v_current_state, '{is_calculating}', 'false'::JSONB);\n        v_current_state := jsonb_set(v_current_state, '{calculation_phase}', '\"none\"'::JSONB);\n    END IF;\n\n    UPDATE participants\n    SET state = v_current_state\n    WHERE user_id = p_user_id;\n\n    -- 7. カードのGraveyard退避処理\n    IF p_room_id IS NOT NULL AND v_current_card IS NOT NULL AND v_current_card->>'status' = 'active' THEN\n        v_card_id := (v_current_card->>'card_id')::INT;\n        -- graveyardが存在しない場合は初期化\n        v_graveyard := COALESCE(v_game_state->'graveyard', '{\"small_deal\":[], \"big_deal\":[], \"market\":[], \"doodad\":[]}'::JSONB);\n        v_deck_graveyard := COALESCE(v_graveyard->v_deck_type, '[]'::JSONB);\n        \n        -- 配列の末尾にカードIDを追加\n        v_deck_graveyard := v_deck_graveyard || to_jsonb(v_card_id);\n        v_graveyard := jsonb_set(v_graveyard, ARRAY[v_deck_type], v_deck_graveyard);\n        \n        v_game_state := jsonb_set(v_game_state, '{graveyard}', v_graveyard);\n        v_game_state := jsonb_set(v_game_state, '{current_card, status}', '\"completed\"'::JSONB);\n\n        UPDATE rooms\n        SET game_state = v_game_state\n        WHERE id = p_room_id;\n    END IF;\n\nEND;\n$function$\n"
  },
  {
    "function_name": "process_special_event",
    "function_definition": "CREATE OR REPLACE FUNCTION public.process_special_event(p_room_id text, p_user_id text, p_event_type text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_room_record RECORD;\n    v_financials JSONB;\n    \n    v_current_cash INT;\n    v_total_income INT;\n    v_total_expenses INT;\n    v_new_cash INT;\n    v_new_cashflow INT;\n    \n    v_children_count INT;\n    v_per_child_expense INT;\n    v_child_care_expense INT;\n    \n    v_charity_cost INT;\nBEGIN\n    -- 1. 手番チェックと部屋データの取得（行ロック）\n    SELECT * INTO v_room_record FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room_record.current_turn_user_id IS NULL OR v_room_record.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    -- 2. プレイヤーデータ取得（行ロック）\n    SELECT id, state INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    v_financials := v_state->'financials';\n    v_current_cash := COALESCE((v_financials->>'cash')::INT, 0);\n    v_total_income := COALESCE((v_financials->'income'->>'total')::INT, 0);\n    v_total_expenses := COALESCE((v_financials->'expenses'->>'total')::INT, 0);\n    v_new_cash := v_current_cash;\n    v_new_cashflow := COALESCE((v_financials->>'cashflow')::INT, 0);\n\n    -- 3. イベント種別ごとの処理\n    IF p_event_type = 'baby' THEN\n        v_children_count := COALESCE((v_state->>'children_count')::INT, 0);\n        IF v_children_count < 3 THEN\n            v_children_count := v_children_count + 1;\n            v_per_child_expense := COALESCE((v_financials->>'per_child_expense')::INT, 0);\n            \n            -- child_care支出の更新\n            v_child_care_expense := COALESCE((v_financials->'expenses'->>'child_care')::INT, 0) + v_per_child_expense;\n            v_financials := jsonb_set(v_financials, '{expenses, child_care}', to_jsonb(v_child_care_expense));\n            \n            -- 総支出とキャッシュフローの再計算\n            v_total_expenses := v_total_expenses + v_per_child_expense;\n            v_new_cashflow := v_total_income - v_total_expenses;\n            \n            v_financials := jsonb_set(v_financials, '{expenses, total}', to_jsonb(v_total_expenses));\n            v_financials := jsonb_set(v_financials, '{cashflow}', to_jsonb(v_new_cashflow));\n            \n            v_state := jsonb_set(v_state, '{children_count}', to_jsonb(v_children_count));\n        END IF;\n\n    ELSIF p_event_type = 'downsized' THEN\n        -- リストラ：総支出額と同額の現金を没収し、2回休み（downsized_turns_left）を付与\n        v_new_cash := v_current_cash - v_total_expenses;\n        v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_new_cash));\n        v_state := jsonb_set(v_state, '{downsized_turns_left}', '2'::jsonb);\n\n    ELSIF p_event_type = 'charity' THEN\n        -- チャリティ：総収入の10%を支払い、3ターンの間効果（charity_turns_left）を付与\n        v_charity_cost := (v_total_income * 0.10)::INT;\n        IF v_current_cash < v_charity_cost THEN\n            RETURN jsonb_build_object('success', false, 'error', '手元資金が足りないためチャリティを行えません。');\n        END IF;\n        v_new_cash := v_current_cash - v_charity_cost;\n        v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_new_cash));\n        v_state := jsonb_set(v_state, '{charity_turns_left}', '3'::jsonb);\n\n    ELSE\n        RETURN jsonb_build_object('success', false, 'error', '無効なイベント種別です。');\n    END IF;\n\n    -- 4. 財務状態と行動ロックの解除を適用\n    v_state := jsonb_set(v_state, '{financials}', v_financials);\n    v_state := jsonb_set(v_state, '{is_calculating}', 'false'::jsonb);\n    v_state := jsonb_set(v_state, '{calculation_phase}', '\"none\"'::jsonb);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE id = v_participant_id;\n\n    RETURN jsonb_build_object(\n        'success', true,\n        'event_type', p_event_type,\n        'new_cash', v_new_cash,\n        'new_cashflow', v_new_cashflow,\n        'children_count', COALESCE((v_state->>'children_count')::INT, 0),\n        'message', '特殊マス（' || p_event_type || '）の処理が完了し、行動ロックが解除されました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "roll_dice_and_move",
    "function_definition": "CREATE OR REPLACE FUNCTION public.roll_dice_and_move(p_room_id text, p_user_id text, p_dice_count integer DEFAULT 1)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_current_turn_user_id TEXT;\n    \n    -- 移動計算用\n    v_current_position INT;\n    v_dice1 INT;\n    v_dice2 INT := 0;\n    v_total_dice INT;\n    v_new_position INT;\n    v_payday_passed BOOLEAN := false;\n    \n    -- 給料日マスの設定\n    v_payday_cells INT[] := ARRAY[0, 5, 11, 18]; \n    v_cell INT;\n    \n    -- 特殊ステータス用\n    v_downsized_turns INT;\n    v_charity_turns INT;\nBEGIN\n    -- 1. 部屋のデータをロックし、現在このプレイヤーの手番であるかを厳格に検証\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    -- 2. プレイヤーのデータをロック\n    SELECT id, state INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    -- 3. 前のアクションが完了せずロック状態になっていないか確認\n    IF (v_state->>'is_calculating')::BOOLEAN THEN\n        RETURN jsonb_build_object('success', false, 'error', '現在の計算フェーズを完了するまで移動できません。');\n    END IF;\n\n    -- 4. リストラ（Downsized）判定\n    v_downsized_turns := COALESCE((v_state->>'downsized_turns_left')::INT, 0);\n    IF v_downsized_turns > 0 THEN\n        -- 休みターンを1つ消費して終了\n        v_downsized_turns := v_downsized_turns - 1;\n        v_state := jsonb_set(v_state, '{downsized_turns_left}', to_jsonb(v_downsized_turns));\n        \n        UPDATE participants SET state = v_state WHERE id = v_participant_id;\n        \n        RETURN jsonb_build_object(\n            'success', true,\n            'skipped', true,\n            'downsized_turns_left', v_downsized_turns,\n            'message', 'リストラ中のため、このターンはお休みです。残り休み回数: ' || v_downsized_turns\n        );\n    END IF;\n\n    -- 5. チャリティ（Charity）判定とダイス数の決定\n    v_charity_turns := COALESCE((v_state->>'charity_turns_left')::INT, 0);\n    IF p_dice_count = 2 AND v_charity_turns > 0 THEN\n        v_dice1 := floor(random() * 6 + 1)::INT;\n        v_dice2 := floor(random() * 6 + 1)::INT;\n        v_total_dice := v_dice1 + v_dice2;\n        v_charity_turns := v_charity_turns - 1;\n        v_state := jsonb_set(v_state, '{charity_turns_left}', to_jsonb(v_charity_turns));\n    ELSE\n        -- チャリティ期間外、または1個を選択した場合\n        v_dice1 := floor(random() * 6 + 1)::INT;\n        v_total_dice := v_dice1;\n    END IF;\n\n    -- 6. 移動後の位置を計算\n    v_current_position := (v_state->>'position')::INT;\n    v_new_position := (v_current_position + v_total_dice) % 24;\n\n    -- 7. 給料日マスの通過・着地判定\n    FOREACH v_cell IN ARRAY v_payday_cells LOOP\n        IF v_current_position + v_total_dice >= 24 THEN\n            IF v_cell > v_current_position OR v_cell <= v_new_position THEN\n                v_payday_passed := true;\n            END IF;\n        ELSE\n            IF v_cell > v_current_position AND v_cell <= v_new_position THEN\n                v_payday_passed := true;\n            END IF;\n        END IF;\n    END LOOP;\n\n    -- 8. データ更新（状態の組み立て）\n    v_state := jsonb_set(v_state, '{last_dice}', to_jsonb(v_total_dice));\n    v_state := jsonb_set(v_state, '{position}', to_jsonb(v_new_position));\n\n    IF v_payday_passed THEN\n        v_state := jsonb_set(v_state, '{is_calculating}', 'true'::jsonb);\n        v_state := jsonb_set(v_state, '{calculation_phase}', '\"payday\"'::jsonb);\n    ELSE\n        v_state := jsonb_set(v_state, '{calculation_phase}', '\"deal\"'::jsonb);\n    END IF;\n\n    -- 9. データベースに永続化\n    UPDATE participants\n    SET state = v_state\n    WHERE id = v_participant_id;\n\n    -- 10. 結果を通知\n    RETURN jsonb_build_object(\n        'success', true,\n        'skipped', false,\n        'dice', v_total_dice,\n        'dice1', v_dice1,\n        'dice2', v_dice2,\n        'charity_turns_left', v_charity_turns,\n        'old_position', v_current_position,\n        'new_position', v_new_position,\n        'payday_passed', v_payday_passed,\n        'message', CASE \n            WHEN v_payday_passed THEN '給料日を通過しました！財務諸表の検算を行ってください（行動ロック中）。'\n            ELSE '移動しました。止まったマスのカードをドローするか、アクションを実行してください。'\n        END\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "roll_dice_and_move",
    "function_definition": "CREATE OR REPLACE FUNCTION public.roll_dice_and_move(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_current_turn_user_id TEXT;\n    \n    -- 移動計算用\n    v_current_position INT;\n    v_dice INT;\n    v_new_position INT;\n    v_payday_passed BOOLEAN := false;\n    \n    -- 給料日マスの設定（board_settings から取得、またはデフォルト [0, 5, 11, 18]）\n    v_payday_cells INT[] := ARRAY[0, 5, 11, 18]; \n    v_cell INT;\nBEGIN\n    -- 1. 部屋のデータをロックし、現在このプレイヤーの手番であるかを厳格に検証\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    -- 2. プレイヤーのデータをロック\n    SELECT id, state INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    -- 3. 前のアクション（財務計算など）が完了せずロック状態になっていないか確認\n    IF (v_state->>'is_calculating')::BOOLEAN THEN\n        RETURN jsonb_build_object('success', false, 'error', '現在の計算フェーズを完了するまで移動できません。');\n    END IF;\n\n    -- 4. サーバーサイドで安全にサイコロを振る（1〜6のランダム生成。チートの余地をゼロに）\n    v_dice := floor(random() * 6 + 1)::INT;\n\n    -- 5. 移動後の位置を計算（ラットレースは24マスの周回ループ 0〜23）\n    v_current_position := (v_state->>'position')::INT;\n    v_new_position := (v_current_position + v_dice) % 24;\n\n    -- 6. 給料日マスの通過・着地判定\n    -- 移動の軌跡（現在のマスから進んだマスまで）の間に給料日マスがあるかを検証\n    FOREACH v_cell IN ARRAY v_payday_cells LOOP\n        IF v_current_position + v_dice >= 24 THEN\n            -- 0番マス（周回またぎ）を越えるケース\n            IF v_cell > v_current_position OR v_cell <= v_new_position THEN\n                v_payday_passed := true;\n            END IF;\n        ELSE\n            -- 通常の直進ケース\n            IF v_cell > v_current_position AND v_cell <= v_new_position THEN\n                v_payday_passed := true;\n            END IF;\n        END IF;\n    END LOOP;\n\n    -- 7. データ更新（状態の組み立て）\n    -- サイコロの出目と新しい位置を格納\n    v_state := jsonb_set(v_state, '{last_dice}', to_jsonb(v_dice));\n    v_state := jsonb_set(v_state, '{position}', to_jsonb(v_new_position));\n\n    -- 給料日を通過していたら、自動で行動ロックをかけ、計算フェーズを 'payday' にセット\n    IF v_payday_passed THEN\n        v_state := jsonb_set(v_state, '{is_calculating}', 'true'::jsonb);\n        v_state := jsonb_set(v_state, '{calculation_phase}', '\"payday\"'::jsonb);\n    ELSE\n        -- 通過していない場合は通常フェーズ（カードを引くなど）へ\n        v_state := jsonb_set(v_state, '{calculation_phase}', '\"deal\"'::jsonb);\n    END IF;\n\n    -- 8. データベースに一撃で永続化\n    UPDATE participants\n    SET state = v_state\n    WHERE id = v_participant_id;\n\n    -- 9. フロントエンドに結果を通知\n    RETURN jsonb_build_object(\n        'success', true,\n        'dice', v_dice,\n        'old_position', v_current_position,\n        'new_position', v_new_position,\n        'payday_passed', v_payday_passed,\n        'message', CASE \n            WHEN v_payday_passed THEN '給料日を通過しました！財務諸表の検算を行ってください（行動ロック中）。'\n            ELSE '移動しました。止まったマスのカードをドローしてください。'\n        END\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "sell_stock_asset",
    "function_definition": "CREATE OR REPLACE FUNCTION public.sell_stock_asset(p_room_id text, p_user_id text, p_stock_symbol text, p_sell_price integer, p_quantity integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_participant_id INT;\n    v_state JSONB;\n    v_room_record RECORD;\n    v_game_state JSONB;\n    v_current_card JSONB;\n    v_card_id INT;\n    v_deck_type TEXT;\n    v_graveyard JSONB;\n    v_deck_graveyard JSONB;\n    \n    v_financials JSONB;\n    v_current_cash INT;\n    v_total_revenue INT;\n    v_new_cash INT;\n    \n    v_stocks_list JSONB;\n    v_existing_stock JSONB;\n    v_current_quantity INT;\n    v_new_quantity INT;\nBEGIN\n    IF p_quantity <= 0 OR p_sell_price <= 0 THEN\n        RETURN jsonb_build_object('success', false, 'error', '株数および売却価格は1以上である必要があります。');\n    END IF;\n\n    SELECT * INTO v_room_record FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room_record.current_turn_user_id IS NULL OR v_room_record.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('success', false, 'error', 'あなたの方番（ターン）ではありません。');\n    END IF;\n\n    v_game_state := v_room_record.game_state;\n\n    SELECT id, state INTO v_participant_id, v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF NOT (v_state->>'is_calculating')::BOOLEAN OR v_state->>'calculation_phase' != 'market' THEN\n        RETURN jsonb_build_object('success', false, 'error', '現在は株式を売却できるフェーズではありません。');\n    END IF;\n\n    v_financials := v_state->'financials';\n    v_stocks_list := v_financials->'assets'->'stocks';\n    \n    IF v_stocks_list IS NULL THEN\n        RETURN jsonb_build_object('success', false, 'error', '株式を一切保有していません。');\n    END IF;\n\n    v_existing_stock := v_stocks_list->p_stock_symbol;\n    IF v_existing_stock IS NULL THEN\n        RETURN jsonb_build_object('success', false, 'error', '指定された銘柄 ' || p_stock_symbol || ' を保有していません。');\n    END IF;\n\n    v_current_quantity := (v_existing_stock->>'quantity')::INT;\n    IF v_current_quantity < p_quantity THEN\n        RETURN jsonb_build_object('success', false, 'error', '売却希望数が保有株数（' || v_current_quantity || '株）を超えています。');\n    END IF;\n\n    v_current_cash := (v_financials->>'cash')::INT;\n    v_total_revenue := p_sell_price * p_quantity;\n    v_new_cash := v_current_cash + v_total_revenue;\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_new_cash));\n\n    v_new_quantity := v_current_quantity - p_quantity;\n    \n    IF v_new_quantity > 0 THEN\n        v_stocks_list := jsonb_set(v_stocks_list, ARRAY[p_stock_symbol, 'quantity'], to_jsonb(v_new_quantity));\n    ELSE\n        v_stocks_list := v_stocks_list - p_stock_symbol;\n    END IF;\n    \n    v_financials := jsonb_set(v_financials, '{assets, stocks}', v_stocks_list);\n\n    v_state := jsonb_set(v_state, '{financials}', v_financials);\n    v_state := jsonb_set(v_state, '{is_calculating}', 'false'::jsonb);\n    v_state := jsonb_set(v_state, '{calculation_phase}', '\"none\"'::jsonb);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE id = v_participant_id;\n\n    -- カードのGraveyard退避処理\n    v_current_card := v_game_state->'current_card';\n    IF v_current_card IS NOT NULL AND v_current_card->>'status' = 'active' THEN\n        v_deck_type := v_current_card->>'deck_type';\n        v_card_id := (v_current_card->>'card_id')::INT;\n        \n        v_graveyard := COALESCE(v_game_state->'graveyard', '{\"small_deal\":[], \"big_deal\":[], \"market\":[], \"doodad\":[]}'::JSONB);\n        v_deck_graveyard := COALESCE(v_graveyard->v_deck_type, '[]'::JSONB);\n        \n        v_deck_graveyard := v_deck_graveyard || to_jsonb(v_card_id);\n        v_graveyard := jsonb_set(v_graveyard, ARRAY[v_deck_type], v_deck_graveyard);\n        \n        v_game_state := jsonb_set(v_game_state, '{graveyard}', v_graveyard);\n        v_game_state := jsonb_set(v_game_state, '{current_card, status}', '\"completed\"'::JSONB);\n\n        UPDATE rooms\n        SET game_state = v_game_state\n        WHERE id = p_room_id;\n    END IF;\n\n    RETURN jsonb_build_object(\n        'success', true,\n        'stock_symbol', p_stock_symbol,\n        'quantity_sold', p_quantity,\n        'total_revenue', v_total_revenue,\n        'remaining_cash', v_new_cash,\n        'remaining_quantity', v_new_quantity,\n        'message', '株式の売却が正常に完了し、カードを退避させました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "start_game_with_shuffled_decks",
    "function_definition": "CREATE OR REPLACE FUNCTION public.start_game_with_shuffled_decks(p_room_id text, p_host_user_id text)\n RETURNS json\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant_count INT;\n    v_player RECORD;\n    v_shuffled_professions JSONB[];\n    v_shuffled_players TEXT[];\n    v_first_turn_user_id TEXT;\n    v_idx INT := 1;\n    v_small_deal JSONB;\n    v_big_deal JSONB;\n    v_market JSONB;\n    v_doodad JSONB;\nBEGIN\n    SELECT COUNT(*) INTO v_participant_count \n    FROM public.participants \n    WHERE room_id = p_room_id;\n\n    IF v_participant_count = 0 THEN\n        RETURN json_build_object('success', false, 'error', '参加者がいません。');\n    END IF;\n\n    SELECT array_agg(jsonb_build_object('name', name, 'financials', financials)) INTO v_shuffled_professions\n    FROM (\n        SELECT * FROM (VALUES \n            ('医師', '{\"cash\": 400, \"cashflow\": 3550, \"income\": {\"salary\": 13200, \"passive\": 0, \"total\": 13200}, \"expenses\": {\"taxes\": 3420, \"mortgage_payment\": 1900, \"car_loan_payment\": 380, \"other\": 3950, \"child_care\": 0, \"total\": 9650}, \"liabilities\": {\"mortgage\": 202000, \"car_loan\": 19000, \"retail_debt\": 160000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 640}'::jsonb),\n            ('航空機パイロット', '{\"cash\": 400, \"cashflow\": 2600, \"income\": {\"salary\": 9500, \"passive\": 0, \"total\": 9500}, \"expenses\": {\"taxes\": 2350, \"mortgage_payment\": 1330, \"car_loan_payment\": 300, \"other\": 2920, \"child_care\": 0, \"total\": 6900}, \"liabilities\": {\"mortgage\": 143000, \"car_loan\": 15000, \"retail_debt\": 23000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 480}'::jsonb),\n            ('弁護士', '{\"cash\": 400, \"cashflow\": 2080, \"income\": {\"salary\": 7500, \"passive\": 0, \"total\": 7500}, \"expenses\": {\"taxes\": 1830, \"mortgage_payment\": 1100, \"car_loan_payment\": 220, \"other\": 2270, \"child_care\": 0, \"total\": 5420}, \"liabilities\": {\"mortgage\": 115000, \"car_loan\": 11000, \"retail_debt\": 85000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 380}'::jsonb),\n            ('エンジニア', '{\"cash\": 400, \"cashflow\": 1690, \"income\": {\"salary\": 4900, \"passive\": 0, \"total\": 4900}, \"expenses\": {\"taxes\": 1050, \"mortgage_payment\": 700, \"car_loan_payment\": 140, \"other\": 1320, \"child_care\": 0, \"total\": 3210}, \"liabilities\": {\"mortgage\": 75000, \"car_loan\": 7000, \"retail_debt\": 17000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 250}'::jsonb),\n            ('ビジネスマネジャー', '{\"cash\": 400, \"cashflow\": 1670, \"income\": {\"salary\": 4600, \"passive\": 0, \"total\": 4600}, \"expenses\": {\"taxes\": 910, \"mortgage_payment\": 700, \"car_loan_payment\": 120, \"other\": 1200, \"child_care\": 0, \"total\": 2930}, \"liabilities\": {\"mortgage\": 75000, \"car_loan\": 6000, \"retail_debt\": 16000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 240}'::jsonb),\n            ('看護婦', '{\"cash\": 480, \"cashflow\": 1120, \"income\": {\"salary\": 3100, \"passive\": 0, \"total\": 3100}, \"expenses\": {\"taxes\": 600, \"mortgage_payment\": 400, \"car_loan_payment\": 100, \"other\": 880, \"child_care\": 0, \"total\": 1980}, \"liabilities\": {\"mortgage\": 47000, \"car_loan\": 5000, \"retail_debt\": 10000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 170}'::jsonb),\n            ('警察官', '{\"cash\": 520, \"cashflow\": 1120, \"income\": {\"salary\": 3000, \"passive\": 0, \"total\": 3000}, \"expenses\": {\"taxes\": 580, \"mortgage_payment\": 400, \"car_loan_payment\": 100, \"other\": 800, \"child_care\": 0, \"total\": 1880}, \"liabilities\": {\"mortgage\": 46000, \"car_loan\": 5000, \"retail_debt\": 3000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 160}'::jsonb),\n            ('教師', '{\"cash\": 400, \"cashflow\": 1110, \"income\": {\"salary\": 3300, \"passive\": 0, \"total\": 3300}, \"expenses\": {\"taxes\": 630, \"mortgage_payment\": 500, \"car_loan_payment\": 100, \"other\": 960, \"child_care\": 0, \"total\": 2190}, \"liabilities\": {\"mortgage\": 50000, \"car_loan\": 5000, \"retail_debt\": 16000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 180}'::jsonb),\n            ('秘書', '{\"cash\": 710, \"cashflow\": 880, \"income\": {\"salary\": 2500, \"passive\": 0, \"total\": 2500}, \"expenses\": {\"taxes\": 460, \"mortgage_payment\": 400, \"car_loan_payment\": 80, \"other\": 680, \"child_care\": 0, \"total\": 1620}, \"liabilities\": {\"mortgage\": 38000, \"car_loan\": 4000, \"retail_debt\": 3000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 140}'::jsonb),\n            ('トラック運転手', '{\"cash\": 750, \"cashflow\": 880, \"income\": {\"salary\": 2500, \"passive\": 0, \"total\": 2500}, \"expenses\": {\"taxes\": 460, \"mortgage_payment\": 400, \"car_loan_payment\": 80, \"other\": 680, \"child_care\": 0, \"total\": 1620}, \"liabilities\": {\"mortgage\": 38000, \"car_loan\": 4000, \"retail_debt\": 3000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 140}'::jsonb),\n            ('機械工', '{\"cash\": 670, \"cashflow\": 720, \"income\": {\"salary\": 2000, \"passive\": 0, \"total\": 2000}, \"expenses\": {\"taxes\": 360, \"mortgage_payment\": 300, \"car_loan_payment\": 60, \"other\": 560, \"child_care\": 0, \"total\": 1280}, \"liabilities\": {\"mortgage\": 31000, \"car_loan\": 3000, \"retail_debt\": 2000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 110}'::jsonb),\n            ('ビルの管理人', '{\"cash\": 560, \"cashflow\": 650, \"income\": {\"salary\": 1600, \"passive\": 0, \"total\": 1600}, \"expenses\": {\"taxes\": 280, \"mortgage_payment\": 200, \"car_loan_payment\": 60, \"other\": 410, \"child_care\": 0, \"total\": 950}, \"liabilities\": {\"mortgage\": 20000, \"car_loan\": 4000, \"retail_debt\": 3000}, \"assets\": {\"stocks\": {}, \"real_estate\": []}, \"per_child_expense\": 70}'::jsonb)\n        ) AS p(name, financials)\n        ORDER BY random()\n    ) AS shuffled;\n\n    IF v_participant_count > array_length(v_shuffled_professions, 1) THEN\n         RETURN json_build_object('success', false, 'error', '参加者数が職業の数を超えています。');\n    END IF;\n\n    SELECT array_agg(user_id) INTO v_shuffled_players\n    FROM (\n        SELECT user_id \n        FROM public.participants \n        WHERE room_id = p_room_id \n        ORDER BY random()\n    ) AS random_players;\n\n    v_first_turn_user_id := v_shuffled_players[1];\n\n    FOR v_player IN (SELECT user_id FROM public.participants WHERE room_id = p_room_id) LOOP\n        UPDATE public.participants\n        SET state = jsonb_set(\n            jsonb_set(\n                jsonb_set(\n                    COALESCE(state, '{}'::jsonb), \n                    '{profession}', \n                    to_jsonb(v_shuffled_professions[v_idx]->>'name')\n                ),\n                '{financials}', \n                v_shuffled_professions[v_idx]->'financials'\n            ),\n            '{children_count}',\n            '0'::jsonb\n        )\n        WHERE room_id = p_room_id AND user_id = v_player.user_id;\n\n        v_idx := v_idx + 1;\n    END LOOP;\n\n    SELECT COALESCE(jsonb_agg(id), '[]'::jsonb) INTO v_small_deal FROM (SELECT id FROM public.cards WHERE deck_type = 'small_deal' ORDER BY random()) s;\n    SELECT COALESCE(jsonb_agg(id), '[]'::jsonb) INTO v_big_deal FROM (SELECT id FROM public.cards WHERE deck_type = 'big_deal' ORDER BY random()) s;\n    SELECT COALESCE(jsonb_agg(id), '[]'::jsonb) INTO v_market FROM (SELECT id FROM public.cards WHERE deck_type = 'market' ORDER BY random()) s;\n    SELECT COALESCE(jsonb_agg(id), '[]'::jsonb) INTO v_doodad FROM (SELECT id FROM public.cards WHERE deck_type = 'doodad' ORDER BY random()) s;\n\n    UPDATE public.rooms\n    SET \n        current_turn_user_id = v_first_turn_user_id,\n        game_state = jsonb_set(\n            jsonb_set(\n                COALESCE(game_state, '{}'::jsonb), \n                '{decks}', \n                jsonb_build_object(\n                    'small_deal', v_small_deal,\n                    'big_deal', v_big_deal,\n                    'market', v_market,\n                    'doodad', v_doodad\n                )\n            ),\n            '{status}',\n            '\"playing\"'::jsonb\n        )\n    WHERE id = p_room_id;\n\n    RETURN json_build_object('success', true, 'message', 'ゲーム開始：山札・手番・職業の抽選が完了し、ゲームが開始されました。');\nEND;\n$function$\n"
  },
  {
    "function_name": "trigger_purge_zombie_rooms",
    "function_definition": "CREATE OR REPLACE FUNCTION public.trigger_purge_zombie_rooms()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\nBEGIN\n    -- 24時間以上経過した rooms レコードを物理削除\n    -- ON DELETE CASCADE 制約により、紐づく participants も連動して自動削除される\n    DELETE FROM rooms WHERE created_at < NOW() - INTERVAL '24 hours';\n    RETURN NEW;\nEND;\n$function$\n"
  }
]
